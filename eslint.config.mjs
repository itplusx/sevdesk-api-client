import unjs from "eslint-config-unjs"

export default unjs({
  ignores: [
    'src/generated'
  ],
  rules: {
    // rule overrides
  },
  markdown: {
    rules: {
      // markdown rule overrides
    },
  },
});
